/*--------------------------------*- C++ -*----------------------------------*\
| Generated by OpenFOAM RC CFD GUI                                           |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    object      U;
}

dimensions      [ 0 1 -1 0 0 0 0 ];
internalField   uniform ( {{ "%.6f" | format(Ux) }} {{ "%.6f" | format(Uy) }} {{ "%.6f" | format(Uz) }} );

boundaryField
{
    inlet
    {
        type        fixedValue;
        value       uniform ( {{ "%.6f" | format(Ux) }} {{ "%.6f" | format(Uy) }} {{ "%.6f" | format(Uz) }} );
    }
    outlet
    {
        type        zeroGradient;
    }
    top
    {
        type        symmetryPlane;
    }
    bottom
    {
        type        symmetryPlane;
    }
    sides
    {
        type        symmetry;
    }
    aircraft
    {
        type        noSlip;
    }
}
